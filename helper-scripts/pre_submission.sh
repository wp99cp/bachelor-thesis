#!/bin/bash

# This script is run before a the task is submitted on Euler. It is used to
# prepare the task for submission, compile the code, transfer the training
# data if changed, etc.

echo "this file is run on the login node"
